import 'package:flutter_storyboard/flutter_storyboard.dart';
import 'package:flutter_storyboard/src/model/storyboard_graph.dart';

class GraphShallowReference {
  final ImageWidgetData? image;
  final StoryboardGraph? graph;

  GraphShallowReference({this.image, this.graph});
}
